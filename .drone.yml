---
kind: pipeline
type: docker
name: linux-amd64

platform:
  arch: amd64
  os: linux

steps:
- name: Start syncing && Compiling
  image: ghcr.io/sushrut1101/docker:arch
  commands:
  - neofetch
  - nproc
  - nproc --all
